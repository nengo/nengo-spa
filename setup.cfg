[aliases]
test = pytest

[bdist_wheel]
universal = 1

[build_sphinx]
source-dir = docs
build-dir = docs/_build
all_files = 1

[check-manifest]
ignore =
    .travis.yml
    .pylintrc
    *.ipynb_checkpoints
    *.ipynb_checkpoints/*
    .ropeproject
    .ropeproject/*

[coverage:run]
source = nengo_spa

[coverage:report]
omit =
    */tests/test*
    nengo_spa/conftest.py
exclude_lines =
    raise NotImplementedError

[flake8]
exclude = __init__.py, compat.py
# E501, F401 are checked by pylint
ignore = E123,E133,E226,E241,E242,E501,E731,F401,W503
max-complexity = 10

[tool:pytest]
norecursedirs = .* *.egg build dist docs *.analytics *.logs *.plots
markers =
    example: Mark a test as an example.
    noassertions: Mark a test without assertions. It will only be run if plots or analytics data are produced.
    slow: Mark a test as slow to skip it per default.
filterwarnings =
    ignore::ImportWarning
    ignore:(Buffer|Memory):DeprecationWarning
    ignore:Skipping some optimization steps
    ignore:SciPy is not installed
    ignore:numpy.(dtype|ufunc) size changed
    ignore::FutureWarning:nengo.utils.numpy
    ignore:IPython.core.inputsplitter:DeprecationWarning
    ignore:Matplotlib is currently using agg
    ignore:Using or importing the ABCs:DeprecationWarning:pyparsing
    ignore:Using or importing the ABCs:DeprecationWarning:nbformat
    ignore:Using or importing the ABCs:DeprecationWarning:nengo
    ignore:The SafeConfigParser class has been renamed:DeprecationWarning:nengo
    ignore:Flags not at the start of the expression:DeprecationWarning:nengo_gui
